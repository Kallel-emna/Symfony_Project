<a href="{{ path('add_classroom') }}">Add Classroom</a>
<br>
<h2>Liste Classroom</h2>
<br>
<table border='1'>
 <tr>
    <td>Identifiant<td>
    <td>Nom</td>
    <td>Description</td>
    <td>Modifier</td>
    <td>Supprimer</td>
 </tr>
{%for c in tabClassroom %}
<tr>
  <td>id:{{c.id}}</td>
  <td>name:{{c.name}}</td>
  <td>description:{{c.description}}</td>

  <td> <a href="{{ path('update_classroom',{'id':c.id}) }}">Update</a></td>
  <td> <a href="{{ path('remove',{'id':c.id}) }}">Remove</a></td>
</tr>
<br>
{%endfor%}
</table>


